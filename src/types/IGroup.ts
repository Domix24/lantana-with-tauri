export interface IGroup {
    active: boolean,
    title: string,
    id: number,
    timers: number[],
  }
  
  export interface IDexieGroup {
    active: boolean,
    title: string,
    timers: number[],
  }